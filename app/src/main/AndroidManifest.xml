<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mmp.wanandroid">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ui.base.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WanAndroid">
        <activity android:name=".ui.system.view.SystemDetailActivity"></activity>
        <activity android:name=".ui.mine.view.RankActivity" />
        <activity android:name=".ui.mine.view.RegisterActivity" />
        <activity android:name=".ui.mine.view.SettingActivity" />
        <activity android:name=".ui.mine.view.TodoActivity" />
        <activity android:name=".ui.mine.view.CollectActivity" />
        <activity android:name=".ui.mine.view.IntegralActivity" />
        <activity android:name=".ui.mine.view.LoginActivity" />
        <activity android:name=".ui.home.view.SearchActivity" />
        <activity android:name=".ui.web.WebActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>